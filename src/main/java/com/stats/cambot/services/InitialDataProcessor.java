package com.stats.cambot.services;

/**
 * To be run if the users lastfm data is not in the local PSQL database. Running this parses their scrobble history
 * and translates it into cambot model. To be used as well as API calls.
 */
public class InitialDataProcessor {
}
